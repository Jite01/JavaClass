The questionnaire has 20 questions... two rows

Create an encompassing string array of 20 string arrays for the 20 columns with two rows

Store the character traits in these arrays

Create an int array for users response of size 20

The int response array will have its variables either of 1 or 0... 

If the first variable in the array is 1, it means the user resonated with the column B, if 0 the user resonated with the column A






